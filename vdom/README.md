# cljsjs/vdom

[](dependency)
```clojure
[cljsjs/vdom "0.1.0-0"]
```
[](/dependency)

After adding the above dependency to your project you can require the packaged library like so:

```clojure
(ns application.core
  (:require cljsjs.vdom))
```
